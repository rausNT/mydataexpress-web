# Совместимость расширений

DataExpress связывает настольную и веб-реализацию расширения по стабильному
контракту: функции — по `Name`, действия — по `Id`. Текущий Web Server выполняет
действие только при наличии соответствующего web-модуля (`WebExists = True`).

## Стратегия бесшовной миграции

1. Не менять `Name` и `Id`: существующие формы и действия продолжат ссылаться на
   те же расширения.
2. Анализировать исходный Pascal Script до развёртывания.
3. Переносимый код автоматически пробовать в штатном Pascal Script Web Server,
   даже если автор ещё не создал парный `.wepas`.
4. Windows-зависимые операции направлять через провайдеры: документы, файловое
   хранилище, запуск задач, внешние API.
5. Пользовательский интерфейс переводить на асинхронные web-события и окна
   сообщений, не пытаясь эмулировать WinAPI в браузере.

## Аудит существующих модулей

```powershell
node tools/extension-audit.mjs C:\path\to\extensions --output compatibility.json
```

Отчёт содержит секцию `runtimeCompatibility`, которая повторяет правила
сопоставления сервера: функции связываются по `Name` без учёта регистра, действия
— по стабильному `Id`. Отдельно перечисляются отсутствующие, дублирующиеся и
«осиротевшие» web-спецификации.

После входа в базу сервер отдаёт тот же эксплуатационный срез по адресу
`?extensioncompat`. Статусы:

- `web-script` — найдена обычная Pascal Script web-реализация;
- `auto-web-script` — парный `.wepas` отсутствовал, но исходный `.epas`
  автоматически прошёл проверку и скомпилировался веб-средой;
- `provider` — все provider-вызовы web-модуля используют настроенные секции
  `[Provider:…]`;
- `provider-unconfigured` — web-модуль найден, но хотя бы одна его provider-секция
  отсутствует в `dxwebsrv.cfg` или не содержит `Url`;
- `provider-unresolved` — имя provider вычисляется динамически и не может быть
  проверено до выполнения;
- `windows-worker-required` — исходный модуль обращается к DLL, COM/OLE,
  WinAPI, сети или прямой файловой системе и должен выполняться изолированно;
- `automatic-compile-failed` — модуль не содержит заранее запрещённых вызовов,
  но веб-компилятор его отклонил; первая ошибка находится в `blockReason`, а
  запуск остальных модулей базы продолжается;
- `missing` — web-реализация отсутствует.

Автоматический fallback не переопределяет написанный автором `.wepas` и
общесерверный проверенный модуль. Сопоставление новых расширений выполняется по
метаданным `Name`/`Id`, поэтому добавлять их имена в исходный код сервера не
требуется. Проверка консервативна: прямые процессы, DLL, COM/OLE, сетевые и
файловые API не запускаются внутри Linux-процесса сервера.

Отсутствующая web-функция возвращает `Null`, а desktop-only действие безопасно
пропускается, чтобы необязательная UI-команда не обрывала составной
`ActionOnCreate` и не оставляла форму с неинициализированными запросами.
Это не скрывает состояние совместимости: `?extensioncompat` показывает mapping
как `missing`, а `summary.complete` остаётся `false`, пока web-модуль или
provider не настроен. Комментарии и строковые литералы с текстом
`ExtensionProviderCall` не создают ложный статус.

## Windows-worker: что берём из Proton

[Valve Proton](https://github.com/ValveSoftware/Proton) подтверждает рабочую
архитектуру запуска Windows-программ поверх Wine, но сам Proton ориентирован на
Steam-игры. DXVK, VKD3D, Vulkan, контроллеры и fullscreen-хаки серверу
DataExpress не требуются.

Для worker используется более узкий набор:

- актуальный Wine с новым WoW64 для запуска 32-битного `dxwebsrv.exe` и DLL в
  64-битном окружении;
- отдельный Wine prefix на базу или комплект расширений;
- systemd sandbox, непривилегированный пользователь, закрытая сеть и явно
  смонтированные каталоги;
- точечные рецепты Winetricks только для конкретной документированной
  зависимости;
- IPC/provider-контракт между Linux web-процессом и worker вместо публикации
  Wine-порта в Интернет.

[Wine 11](https://list.winehq.org/hyperkitty/list/wine-announce%40list.winehq.org/2026/1/)
объявляет новый WoW64 полностью поддержанным и отказывается от чистых 32-битных
prefix. Поэтому POC должен использовать 64-битный prefix с `WINEARCH=wow64`, а
не старую схему `WINEARCH=win32`. Идея контейнерного воспроизводимого окружения
берётся из [Steam Runtime](https://github.com/ValveSoftware/steam-runtime), но
сам игровой runtime на сервер не устанавливается.

## Генерация web-модуля и manifest

```powershell
node tools/extension-migrate.mjs OfficeTools.epas
```

Одна команда создаёт согласованный комплект:

- `OfficeTools.wepas` — штатный web-модуль Pascal Script;
- `OfficeTools.manifest.json` — машинный контракт операций;
- `OfficeTools.provider.mjs` — Node-provider с handler-заглушками, если он нужен;
- `OfficeTools.provider.cfg.example` — секция для `dxwebsrv.cfg`, если есть
  provider-backed операции.
- `OfficeTools.provider.env.example` — окружение процесса provider: тот же
  Bearer-токен, порт и политика встроенных рецептов.

Мигратор анализирует каждую routine отдельно. Самодостаточная реализация без
Windows/OLE/DLL/UI/file findings и неизвестных globals копируется в `.wepas` со
статусом `web-script`. Безопасные локальные helper-routines включаются один раз
в порядке зависимостей. Доступны классы, типы, constants, methods и properties,
фактически зарегистрированные в `CompilerDecls`; desktop-вызовы `EvalExpr`,
`SQLSelect` и `SQLExecute` при переносе становятся вызовами через `Session`.
Сетевой код может переноситься напрямую, но получает `reviewRequired`, чтобы
политика URL, таймаутов и секретов была проверена явно. Остальные поддерживаемые
сигнатуры получают provider-адаптер. Анализ консервативен: неизвестная
зависимость не считается portable только потому, что её реализация не найдена.
Флаг `--all-providers` отключает inline-перенос для осознанной изоляции всех
операций за HTTP.

Для старой одноаргументной функции `HTTP_GET(URL): String`, где OLE используется
в helper-коде обработки URL, генерируется защищённый кроссплатформенный
`http-get` recipe. Он уже реализован и не увеличивает
`providerImplementationsRequired`; обязательный allow-list и остальные
параметры записываются в `.provider.env.example`.

Для форумного `SendHttpRequest` версии 1.3 распознаются action со стабильным
`Id=B2C1C477-85A4-4133-9D9C-0FD61CA10F1C` и пятиаргументная
`SendHttpRequestFunction`. Оба mappings получают готовый `http-request` recipe.
В сгенерированном `.wepas` остаётся вычисление выражений и запись
`request_result`, а `THttpClient` заменяется provider-вызовом. Поэтому
allow-list, запрет private/небезопасных URL, redirects, таймаут и лимиты
request/response действуют централизованно без изменения пользовательского
`Name`/`Id`.

Для форумного модуля DaData мигратор распознаёт `DA_FIRM_GET`, `DA_BANK_GET` и
`DA_ADDR_GET` и создаёт `dadata-suggest` recipe. Провайдер вызывает официальный
HTTPS API с `count=1`, возвращает совместимый `SuggestResponse` XML и state bundle.
Сгенерированный `.wepas` применяет bundle через `Session.SetExprVar`, поэтому
старые функции `DA_FIRM_NAME`, `DA_BANK_FIELD`, `DA_ADDR_FIELD` и остальные
читают те же значения, что после OLE-вызова. Отсутствующие поля предварительно
сбрасываются в `Null`, чтобы результат прошлого запроса не протекал в новый.

Для проверенного форумного модуля Word/Excel распознаются точные desktop-routines
`convert_Word` и `convert_Excel`. Их стабильные action `Id` сохраняются, а
manifest получает `office-document-convert` recipe для Writer или Calc.
Сгенерированный handler выполняет конвертацию через LibreOffice в headless-режиме,
возвращает прежний Boolean и не увеличивает `providerImplementationsRequired`.
Каталоги входных и выходных файлов обязательно ограничиваются переменными
`DX_OFFICE_INPUT_ROOTS` и `DX_OFFICE_OUTPUT_ROOTS`; подробная настройка приведена
в `provider-api.md`.

По умолчанию комплект записывается рядом с исходным `.epas`. Параметр `--output`
может выбрать другой каталог, но имя web-модуля обязано оканчиваться на `.wepas`,
чтобы файл можно было импортировать в DataExpress как web-реализацию.

Создание provider-файлов можно отключить `--no-provider`, а manifest и provider —
`--no-manifest`. Повторно создать provider по существующему manifest можно так:

```powershell
npm run scaffold:provider -- OfficeTools.manifest.json
```

Если manifest содержит provider-backed операции, в комплект также записывается
`dataexpress-provider-sdk.mjs`. Поэтому provider не зависит от расположения
исходного репозитория и может быть скопирован на другой компьютер вместе с
manifest и `.provider.mjs`. Для полностью inline-модуля эти sidecars не создаются.

## Пакетная миграция каталога

Штатный формат DataExpress требует одновременно импортировать desktop-модуль
`.epas` и его web-реализацию `.wepas`. Для набора экспортированных расширений:

```powershell
npm run migrate:extensions -- C:\extensions --output-dir C:\extensions-web
```

Инструмент рекурсивно обходит каталог и создаёт отдельный переносимый комплект:

- копирует исходные `.epas` и уже существующие `.wepas` без изменения;
- генерирует адаптеры только для полностью отсутствующих web-реализаций;
- переносит чистые routines напрямую и создаёт provider только при необходимости;
- не создаёт второй модуль при частичном покрытии `Name`/`Id`;
- блокирует коллизии имён provider и дублирующиеся desktop/web mapping;
- назначает необходимым providers последовательные localhost-порты начиная с `9081`;
- пишет `migration-index.json` и, при наличии providers, общий
  `dxwebsrv.providers.cfg.example`.
- создаёт для каждого provider собственный `.provider.env.example`.

Непустой выходной каталог отклоняется, поэтому в результат не попадут устаревшие
файлы предыдущей миграции. `--start-port 12000` меняет первый порт. В режиме
`--strict` команда возвращает ненулевой код при ручных адаптациях, частичном
покрытии, коллизиях, некорректных метаданных или ещё не реализованных provider
handlers. Сгенерированный scaffold намеренно не считается готовой реализацией;
после его реализации готовность проверяется командой `preflight:provider`.

## Проверка готового комплекта

Offline-проверка не запускает и не импортирует JavaScript-файлы provider:

```powershell
npm run verify:extension-bundle -- C:\extensions-web --offline
```

Она проверяет `migration-index.json`, что все ссылки остаются внутри комплекта,
наличие `.epas`, `.wepas`, manifest, config, provider SDK и scaffold, отсутствие
незавершённых handler markers, а затем заново строит совместимость по `Name`/`Id`.
Отметки внутри комментариев не засчитываются. Offline-режим сообщает, что
сетевой статус providers не проверен.

Для эксплуатационного допуска providers должны быть запущены, а verifier —
получить фактический `dxwebsrv.cfg`:

```powershell
npm run verify:extension-bundle -- C:\extensions-web --config C:\server\dxwebsrv.cfg
```

В live-режиме для каждого provider, найденного в `.wepas`, требуется manifest и
выполняется проверка Bearer-токена, `/health`, `/capabilities` и полного набора
операций. Токены не включаются в JSON-отчёт. Ненулевой код возврата означает,
что комплект пока нельзя считать полностью готовым.

Manifest
фиксирует имя провайдера, исходный/web-модули, стабильные `Name`/`Id`, операции и
статусы всех сопоставлений. Если объявление процедуры или функции не найдено,
элемент получает статус `manual`, а `summary.complete` остаётся `false`.

Генератор автоматически сохраняет JSON-типы аргументов и подбирает runtime-
адаптер результата для строк, Boolean, целых и вещественных чисел, Currency,
`TDateTime` и Variant. Параметры `var`/`out`, объекты, записи и пользовательские
типы могут выполняться напрямую в `web-script`, если их runtime известен
Pascal Script. Но они остаются `manual`, когда routine приходится выносить за
HTTP: provider-вызов сам по себе не может безопасно воспроизвести изменение
аргумента по ссылке или жизненный цикл объекта. Причина и полный список
ограничений каждого сопоставления записываются в `reason`/`issues` manifest.

Расширения `.epas` считаются desktop-модулями, а `.wepas` — их web-реализациями,
как ожидает DataExpress. Аудитор обходит оба типа файлов и может использоваться
как блокирующая проверка перед импортом:

```powershell
node tools/extension-audit.mjs C:\path\to\extensions --config C:\path\to\dxwebsrv.cfg --strict
```

С параметром `--config` строгий аудит также требует существующую секцию
`[Provider:Имя]` с допустимым `Url` для каждого литерального provider-вызова.
Токены и адреса не копируются в JSON-отчёт.

Строгий режим возвращает ненулевой код, если отсутствует парная web-реализация,
есть дублирующийся/лишний mapping либо метаданные не соответствуют типу файла
(например, `OrigName` внутри `.wepas`). Пустой каталог и каталог без объявлений
функций/действий также не считаются успешно проверенной миграцией.

Отчёт выделяет OLE/COM, DLL, ShellExecute, WinAPI, локальные пути, диалоги и
сетевые обращения. Категории:

- `portable` — можно переносить в web-модуль напрямую;
- `review` — требуется адаптация интерфейса или политики доступа;
- `requires-provider` — нужна серверная кроссплатформенная реализация.

OLE и WinAPI нельзя безопасно и одинаково воспроизвести в Linux и браузере.
Совместимость достигается сохранением контракта расширения и заменой реализации,
а не запуском Windows DLL на сервере.

## Отчёт по каталогу desktop-модулей

```powershell
npm run report:extension-corpus -- C:\path\to\extensions --output corpus-report.json --strict
```

Отчёт рекурсивно обрабатывает только `.epas`, не копирует текст исходников и
суммирует `webScript`, `provider`, `manual` и `reviewRequired`. `--strict`
блокирует пустой или неполный результат, а `--strict-review` дополнительно
блокирует сетевые mappings до ручной проверки. Контрольная выборка, ссылки,
хэши архивов и полученный результат описаны в
[`forum-extension-corpus.md`](forum-extension-corpus.md).

## Что обнаружено в каталоге расширений

На 23 июля 2026 года публичный каталог содержит 191 строку расширений. Основные
классы совместимости:

| Класс | Примеры | Web-реализация |
|---|---|---|
| Чистые вычисления | строки, суммы, проверки, даты | Pascal Script без изменений алгоритма |
| HTTP/API | DaData, Telegram, QR-сервисы | серверный HTTP-провайдер |
| Документы | Excel/Calc, Word, конвертация | генерация файлов на сервере без OLE |
| Файлы | хранилище, мультивыгрузка | объектное/локальное серверное хранилище и download API |
| Оборудование | сканеры WIA/TWAIN, Bluetooth | локальный companion-agent с защищённым web API |
| Нативная графика | DLL диаграмм, штрихкоды | SVG/Canvas или серверный генератор |
| Desktop UI | меню, окна, управление компонентами | события и компоненты браузерного клиента |

Companion-agent нужен только там, где браузер по модели безопасности не имеет
прямого доступа к USB/WIA/TWAIN или другому локальному оборудованию. Обычные
вычисления, файлы, документы и HTTP-интеграции должны работать без Windows.

Сетевой контракт и конфигурация провайдеров описаны в `provider-api.md`.
